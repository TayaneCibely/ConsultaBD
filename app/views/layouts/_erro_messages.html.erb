<% if object.errors.any? %>
  <div id="error_explanation">
    <h2>Encontramos <%= pluralize(object.errors.count, "error") %>:</h2>

    <ul>
      <% object.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  </div>
<% end %>